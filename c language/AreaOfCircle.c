#include<stdio.h>
/*
Area of a Circle
*/
int main() {
    float r,pi=3.14;
    printf("Radius of the circle is:");
    scanf("%f",&r);
    printf("Are of the given circle is: %f", r*pi*r);
    return 0;
}