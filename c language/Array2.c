//Traversing array using pointer;
#include<stdio.h>
#include<conio.h>

int fib(int arr,int n);
int show(int arr);

int main(){
    int n;
    printf("Enter the N numbers=");
    scanf("%d \n",%n);
    int arr[];
    fib(int arr,int n);
    show(int arr);

}

int fib(int arr,int n){
    int a=0,b=0,c;
    for(int i==0;i<n-2;i++){
        
    }
}